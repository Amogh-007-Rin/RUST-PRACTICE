[package]
name = "module-064-exercises"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.9.0", features = ["sqlite", "runtime-tokio", "migrate", "derive"] }

[dev-dependencies]
tokio = { version = "1.53.1", features = ["macros", "rt-multi-thread"] }
