//! Integer helpers — the `math` module of this crate.

/// Adds two integers.
pub fn add(a: i32, b: i32) -> i32 {
    // TODO(module-009): return `a + b`.
    let _ = (a, b);
    panic!("TODO(module-009): implement math::add")
}

/// Subtracts `b` from `a`.
pub fn sub(a: i32, b: i32) -> i32 {
    // TODO(module-009): return `a - b`.
    let _ = (a, b);
    panic!("TODO(module-009): implement math::sub")
}

/// Multiplies two integers.
pub fn mul(a: i32, b: i32) -> i32 {
    // TODO(module-009): return `a * b`.
    let _ = (a, b);
    panic!("TODO(module-009): implement math::mul")
}
