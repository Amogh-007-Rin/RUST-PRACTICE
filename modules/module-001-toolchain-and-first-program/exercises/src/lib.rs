//! Module 001: exercise scaffold.
//!
//! Fill in the TODOs below so the integration tests in `tests/` pass, then run
//! the binary with `cargo run -p module-001-exercises`.

/// Returns a greeting for the given name, e.g. `greet("Ada")` -> `"Hello, Ada!"`.
pub fn greet(name: &str) -> String {
    // TODO(module-001): build the greeting with `format!` and return it, e.g.
    // `format!("Hello, {name}!")`.
    let _ = name;
    panic!("TODO(module-001): implement greet")
}

/// Returns the length in bytes of the given message.
pub fn message_length(message: &str) -> usize {
    // TODO(module-001): return the byte length of `message` with `message.len()`.
    let _ = message;
    panic!("TODO(module-001): implement message_length")
}
