[package]
name = "module-043-exercises"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.53.1", features = ["rt", "rt-multi-thread", "macros", "fs", "net", "io-util", "time"] }
